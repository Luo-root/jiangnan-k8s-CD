# role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: default  # 替换为你的命名空间
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]  # 权限根据需求调整